// Tests for shared/types.ts live in backend/src/shared-types.test.ts.
// The shared/ workspace has no independent package.json or test runner —
// it is consumed via tsconfig paths by backend/ and frontend/.
// Run: npm --workspace backend run test -- src/shared-types.test.ts
